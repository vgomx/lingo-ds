Settings-row toggle. Track turns mint (`--success`) when on so it never competes with violet actions.

```jsx
<Switch label="Daily reminder" hint="20:00, every day" defaultChecked />
```

Renders label left / switch right and fills its container — drop it straight into a settings list.
