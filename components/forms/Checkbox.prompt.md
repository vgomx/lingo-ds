Rounded 20px checkbox with a heavy white tick on violet.

```jsx
<Checkbox label="Autoplay audio" hint="Plays the native recording when a card flips" defaultChecked />
```

Multi-select lists and settings only; a single on/off setting row uses `Switch`.
