Native select in brand clothing, for short closed lists (language, deck, interval).

```jsx
<Select label="Language" options={['Spanish', 'Japanese', 'Turkish']} />
```

Use `Tabs` instead when there are 2–4 options and the choice drives the view.
