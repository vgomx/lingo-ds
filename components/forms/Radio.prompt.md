One exclusive choice. Selected state is a thick violet inset ring, not a dot glyph.

```jsx
<Radio label="Spaced repetition" hint="Recommended" checked value="srs" onChange={setMode} />
<Radio label="Cram" value="cram" onChange={setMode} />
```

Group 2–5 in a vertical stack with 12px gaps; above that use `Select`.
