Names an icon-only control and shows its shortcut.

```jsx
<Tooltip label="Shuffle deck" shortcut="S"><IconButton label="Shuffle"><Icon name="shuffle" size={16} /></IconButton></Tooltip>
```

Never put essential information in a tooltip alone.
