Switches views inside a pane.

```jsx
<Tabs items={[{value:'due',label:'Due',count:12},{value:'all',label:'All cards'}]} value={tab} onChange={setTab} />
<Tabs variant="pill" items={['Week','Month','All time']} value={range} onChange={setRange} />
```
