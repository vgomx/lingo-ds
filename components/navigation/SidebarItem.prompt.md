Row in the tool sidebar. Active state is a violet tint plus brighter text — never a left border.

```jsx
<SidebarItem icon={<Icon name="layers" size={18} />} label="Flashcards" meta="12" active />
<SidebarItem icon={<Icon name="git-branch" size={18} />} label="Etymology Explorer" />
```
