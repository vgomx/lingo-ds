The four-grade SRS answer row, revealed after a flip.

```jsx
<ReviewRating onGrade={(key) => next(key)} />
```

Order and colour are fixed: Again (red) · Hard (neutral) · Good (mint) · Easy (violet), with 1-4 shortcuts and the next interval underneath.
