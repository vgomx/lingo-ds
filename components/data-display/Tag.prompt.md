Content label: topic, part of speech, register.

```jsx
<Tag color="var(--tool-etymology)">noun</Tag>
<Tag color="var(--cyan-500)" variant="solid" onRemove={drop}>food</Tag>
```

Soft is the default; solid only when the tag is the loudest thing in a row.
