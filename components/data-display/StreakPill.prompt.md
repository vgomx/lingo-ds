Streak counter. The only component allowed a glow.

```jsx
<StreakPill days={26} />
<StreakPill days={0} active={false} />
```

Sits in the top bar and on the home hero. Never invent a second colour for it — always amber.
