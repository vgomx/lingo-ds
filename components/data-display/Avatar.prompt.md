Member or language avatar, round, with optional presence dot.

```jsx
<Avatar name="Mara Okafor" status="online" />
<Avatar flag="ES" size="lg" />
```

Initials fall back to a deterministic accent hue, so the same name always gets the same colour.
