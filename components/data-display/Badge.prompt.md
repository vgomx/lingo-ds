Tiny uppercase status marker.

```jsx
<Badge tone="brand">New</Badge>
<Badge tone="success" dot>Mastered</Badge>
```

Badge = state, `Tag` = content. Never use it for counts on the rail — that's `RailTile unread`.
